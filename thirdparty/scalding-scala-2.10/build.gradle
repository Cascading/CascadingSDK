/*
 * Copyright (c) 2007-2013 Concurrent, Inc. All Rights Reserved.
 *
 * Project and contact information: http://www.concurrentinc.com/
 */

fetchLatest {
  typePath = "thirdparty/binary"
  latestURL = "http://${rootProject.sourceBucket}/thirdparty/scalding-scala-2.10/latest.txt"
}

fetchLatest << {

  typeDir.listFiles()[ 0 ].renameTo( new File( typeDir, 'scalding-scala-2.10' ) )
}

distCopy << {
  fileTree( rootProject.distDir )
          .include( '**/scripts/scald.rb' )
          .visit { element -> element.getFile().setExecutable( true, false )}
}
